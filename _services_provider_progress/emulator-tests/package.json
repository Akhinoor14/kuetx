{
  "name": "kuetx-services-provider-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Firestore Emulator rules tests for the Services/Provider marketplace feature (SERVICES_PROVIDER_PLAN.md). Covers providers/{uid} (Phase 1), services/{serviceId}+bookings (Phase 2), and bookingAlerts/{uid}/items (Phase 3 Part 2) in one suite, per PROGRESS.md's own note that these three should be tested together rather than as three separate efforts.",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore \"mocha --timeout 20000\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "mocha": "^10.7.0"
  }
}
